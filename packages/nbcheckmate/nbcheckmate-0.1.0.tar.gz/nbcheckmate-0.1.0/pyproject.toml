[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
dynamic = ["dependencies"]
name = "nbcheckmate"
version = "0.1.0"
description = "Library to test jupyter notebooks"
authors = [
  { email = "paul.chupot@gmail.com" },
  { email = "aloys.bernard@gmail.com" },
]

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }
