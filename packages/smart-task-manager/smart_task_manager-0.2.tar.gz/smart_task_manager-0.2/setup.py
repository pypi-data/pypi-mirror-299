# setup.py
from setuptools import setup, find_packages

setup(
    name='smart_task_manager',
    version='0.2',  # Increment the version number
    packages=['smart_task_manager'],
    install_requires=[],
)
