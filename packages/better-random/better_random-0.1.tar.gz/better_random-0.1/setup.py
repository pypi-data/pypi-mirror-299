from setuptools import setup, find_packages

setup(
    name='better_random',
    version='0.1',
    description='A library with a hidden secret',
    author='Johannes Schießl',
    author_email='contact.johannes@icloud.com',
    packages=find_packages(),
    install_requires=[],
)
