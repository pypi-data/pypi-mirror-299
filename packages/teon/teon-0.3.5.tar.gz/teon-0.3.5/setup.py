from setuptools import setup,find_packages

setup(
    name = "teon",
    version = "0.3.5",
    packages = find_packages(),
    author_email = 'mickeyyess2@gmail.com',
    install_requires = [
        'pygame'
    ]
)