# setup.py

from setuptools import setup, find_packages

setup(
    name='isee_club',
    version='0.1.0',
    packages=find_packages(),
    description='A first library',
    author='isee',
    author_email='',
    url='',  # GitHub 链接
    install_requires=[],  # 如果有依赖项，列在这里
)
