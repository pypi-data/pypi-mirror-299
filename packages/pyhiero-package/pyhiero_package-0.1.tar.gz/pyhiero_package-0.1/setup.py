from setuptools import setup, find_packages

setup(
    name='pyhiero_package',  # Your new unique package name
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
    # Other metadata (author, description, etc.)
)
