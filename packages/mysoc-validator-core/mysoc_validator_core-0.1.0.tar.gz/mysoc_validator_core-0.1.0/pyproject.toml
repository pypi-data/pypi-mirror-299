[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
# "extension-module" tells pyo3 we want to build an extension modul
features = ["pyo3/extension-module"]
manifest-path = "mysoc_validator_core/Cargo.toml"
