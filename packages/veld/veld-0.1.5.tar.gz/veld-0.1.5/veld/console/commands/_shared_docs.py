# -*- coding: utf-8 -*-

"""File to store shared documentation for the commands

"""

FLAG_DOCS = {
    "rowwise": (
        "By default Veld applies operators column-wise, retaining the "
        "dimension of the input values. With the --rowwise option the "
        "operation is performed on individual rows (i.e., lines) instead, so "
        "an output is returned for each line of data in the input."
    )
}
